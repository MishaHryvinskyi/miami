const refs = {
    btnOpen: document.getElementById('open'),
    btnClose: document.getElementById('close'),
    menu: document.getElementById('menu'),
    menuList: document.getElementById('menu-list'),
    menuLink: document.getElementById('menu-limk')
};

export default refs;